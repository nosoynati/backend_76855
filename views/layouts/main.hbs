<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css" rel="stylesheet"
    integrity="sha384-EVSTQN3/azprG1Anm3QDgpJLIm9Nao0Yz1ztcQTwFspd3yD65VohhpuuCOmLASjC" crossorigin="anonymous">
  <link rel="stylesheet" href="/css/yourStyles.css">

  <title>{{#if title}}{{title}} — {{/if}}Backend 2</title>
  <style>
    :root {
      color-scheme: light dark;
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }

    body {
      font-family: system-ui, -apple-system, Segoe UI,Helvetica, Roboto, , Arial, sans-serif;
      margin: 1rem;
    }
    .main {
      margin: auto;
    }
    .container {
      display: flex;
      flex-direction: column;
    }

    header {
      display: flex;
      align-items: center;
      justify-content: space-between;
      margin-bottom: 0;
    }

    table {
      border-collapse: collapse;
      margin: auto;
      width: 100%;
    }

    th,
    td {
      border-bottom: 1px solid #9993;
      padding: 8px 10px;
      text-align: center;
    }

    th {
      font-weight: 600;
    }

    .badge {
      padding: 2px 8px;
      border-radius: 999px;
      font-size: 12px;
      border: 1px solid #9996;
    }

    .pill {
      display: inline-block;
      margin-right: 6px;
    }

    nav a {
      margin-right: 10px;
    }

    .pagination a {
      margin: 0 4px;
      text-decoration: none;
    }

    .pagination .current {
      font-weight: bold;
    }

    .toolbar {
      display: flex;
      gap: 8px;
      align-items: center;
      margin-bottom: 10px;
    }

    input,
    select,
    button {
      padding: 6px 10px;
    }
  </style>
</head>
<body>
  {{{body}}}
</body>
</html>